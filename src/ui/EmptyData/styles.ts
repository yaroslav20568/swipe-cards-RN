import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
	container: {
		alignItems: 'center',
		gap: 10,
	},
	title: {
		color: '#fff',
		fontSize: 20,
		fontWeight: 500,
	},
});
