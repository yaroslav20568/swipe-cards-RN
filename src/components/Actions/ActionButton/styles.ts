import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
	actionButton: {
		width: 50,
		height: 50,
		backgroundColor: '#fff',
		borderRadius: '50%',
		alignItems: 'center',
		justifyContent: 'center',
	},
});
